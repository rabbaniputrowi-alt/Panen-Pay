# Panen-Pay
A Grade weigh collection system for cooperative chili collection points.

Photo -> OpenAI gpt 4.o grades its freshness -> HX711 + Load cell weighs it in -> ESP32 processes it -> deterministic engine prices -> The farmer gets a QR code and certificate of transaction
